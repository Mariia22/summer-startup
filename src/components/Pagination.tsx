import React from 'react';
import { Pagination } from '@mantine/core';

const PaginationBlock = () => {
  return <Pagination total={3} sx={{ justifyContent: 'center', marginTop: '12px' }} />;
}

export default PaginationBlock
